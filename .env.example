# Urlix environment example
# Creator: Blitz (blitzlabx)

# Port (Render sets this automatically)
PORT=8080

# Optional future overrides (currently read from lua/config.lua defaults)
# RATE_LIMIT_MAX=30
# RATE_LIMIT_WINDOW=60
# MAX_REDIRECTS=10
# FETCH_TIMEOUT_READ=10
